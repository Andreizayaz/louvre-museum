import { FC, ReactElement } from 'react';

import { OutputForm } from './OutputForm';

import './OutputForm.scss';

const OutputFormContainer: FC = (): ReactElement => <OutputForm />;

export default OutputFormContainer;
OutputFormContainer.displayName = 'OutputFormContainer';
