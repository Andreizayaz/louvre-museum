import { FC, ReactElement } from 'react';

import { Gallery } from './Gallery';

const GalleryContainer: FC = (): ReactElement => <Gallery />;

export default GalleryContainer;
GalleryContainer.displayName = 'GalleryContainer';
