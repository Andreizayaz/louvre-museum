import { FC, ReactElement } from 'react';

import { Header } from './components/header';

import './App.scss';

const App: FC = (): ReactElement => (
  <>
    <Header />
  </>
);

export default App;
